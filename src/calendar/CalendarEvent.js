import React from 'react'

export default class CalendarEvent extends React.Component {
  render () {
    return (
      <div>
        {this.props.title}
        {event.distance}
      </div>
    )
  }
}
